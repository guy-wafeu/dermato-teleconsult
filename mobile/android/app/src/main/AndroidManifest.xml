<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Pas de android.permission.CAMERA : react-native-image-picker ouvre l'appli
         appareil photo du système via un intent (elle porte sa propre permission),
         notre appli n'y accède jamais directement. La déclarer ici forcerait une
         demande d'autorisation à l'exécution que le code ne fait pas — voir la note
         du README de la librairie — et provoque l'échec de "Prendre une photo". -->
    <!-- READ_MEDIA_IMAGES (Android 13+) remplace READ_EXTERNAL_STORAGE pour la
         galerie ; les deux sont déclarées pour couvrir les versions antérieures. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
